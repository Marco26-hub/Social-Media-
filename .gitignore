.DS_Store
node_modules/
.next/
.env.local
.env.*.local
*.env
out/
dist/
tsconfig.tsbuildinfo
public/uploads/

# demo walkthrough output
demo/

# graphify knowledge-graph output (local tooling)
graphify-out/

# analisi commerciale riservata, conservata solo in locale
COMPETITOR-REPORT.md
COPY-SUGGESTIONS.md

# stray local env dumps — never commit
env.download
.vercel

# output dei run Playwright (npm test / npm run test:render)
test-results
playwright-report

# bundle Remotion pre-buildato (artefatto di build)
.remotion-bundle
